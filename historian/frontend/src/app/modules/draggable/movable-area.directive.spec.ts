import { MovableAreaDirective } from './movable-area.directive';

describe('MovableAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new MovableAreaDirective();
    expect(directive).toBeTruthy();
  });
});
